package com.javacafe.ems.config;

/*@Configuration
@EnableWebSecurity*/

public class SecurityConfig {
	
	/*
	 * @Autowired private UserDetailsService usrDetailsService;
	 * 
	 * @Bean public SecurityFilterChain securityFilterChain(HttpSecurity http)
	 * throws Exception {
	 * 
	 * return http.csrf(customizer -> customizer.disable()).
	 * authorizeHttpRequests(request -> request.anyRequest().authenticated()).
	 * httpBasic(Customizer.withDefaults()). sessionManagement(session ->
	 * session.sessionCreationPolicy(SessionCreationPolicy.STATELESS)).build();
	 * 
	 * }
	 * 
	 * @Bean public AuthenticationProvider authenticationProvider() {
	 * DaoAuthenticationProvider provider = new DaoAuthenticationProvider();
	 * provider.setPasswordEncoder(new BCryptPasswordEncoder());
	 * provider.setUserDetailsService(usrDetailsService);
	 * 
	 * return provider; }
	 */
	 
}
